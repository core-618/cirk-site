---
import Layout from '../layouts/Layout.astro';
---

<Layout>
  <!-- NAV -->
  <nav>
    <div class="nav-inner">
      <span class="nav-logo mono">CIRK</span>
      <div class="nav-links">
        <a href="#model">Model</a>
        <a href="#execution">Execution</a>
        <a href="#examples">Examples</a>
        <a href="#faq">FAQ</a>
        <a href="https://github.com/core-618/cirk" target="_blank" rel="noopener">GitHub →</a>
      </div>
    </div>
  </nav>

  <!-- HERO -->
  <section class="hero">
    <div class="container hero-container">
      <div class="hero-status mono">
        <span class="hero-status-dot"></span>
        YOU HAVE 0 OPEN COMPLIANCE EVENTS
      </div>

      <h1>Stop estimating.<br />Start controlling AI execution.</h1>
      <p class="hero-clarify">CIRK replaces story points with a model that defines how AI should execute, review, and deploy work.</p>

      <!-- Model summary -->
      <div class="hero-model">
        <div class="hero-dim"><span class="hero-dim-letter accent mono">C</span><span class="hero-dim-name">Context</span><span class="hero-dim-desc">How much context does the AI need?</span></div>
        <div class="hero-dim"><span class="hero-dim-letter mono" style="color:var(--green)">I</span><span class="hero-dim-name">Iteration</span><span class="hero-dim-desc">How many iterations until it works?</span></div>
        <div class="hero-dim"><span class="hero-dim-letter mono" style="color:var(--amber)">R</span><span class="hero-dim-name">Review</span><span class="hero-dim-desc">How much human review is required?</span></div>
        <div class="hero-dim"><span class="hero-dim-letter mono" style="color:var(--purple)">K</span><span class="hero-dim-name">Risk</span><span class="hero-dim-desc">How dangerous is deploying this?</span></div>
      </div>

      <!-- Live example -->
      <div class="hero-example mono">
        <p class="hero-ex-task">Task: fix bug in payment calculation</p>
        <p class="hero-ex-vector">
          <span class="accent">C: 3</span>
          <span class="hero-ex-sep">|</span>
          <span style="color:var(--green)">I: 2</span>
          <span class="hero-ex-sep">|</span>
          <span style="color:var(--amber)">R: 3</span>
          <span class="hero-ex-sep">|</span>
          <span style="color:var(--purple)">K: 3</span>
        </p>
        <p class="hero-ex-result">→ requires human review + controlled deployment</p>
      </div>

      <!-- Tagline + CTA -->
      <p class="hero-tagline">AI writes the code. CIRK governs the execution.</p>
      <div class="hero-cta">
        <a href="#model" class="btn-primary">Read the spec</a>
        <a href="https://github.com/core-618/cirk" class="btn-ghost" target="_blank" rel="noopener">GitHub →</a>
      </div>
    </div>
  </section>

  <!-- QUICK EXPLANATION -->
  <section class="section contrast-section">
    <div class="container narrow center">
      <div class="contrast-block">
        <div class="contrast-item">
          <p class="contrast-label mono">Story points ask</p>
          <p class="contrast-question">"How long will this take?"</p>
        </div>
        <div class="contrast-divider"></div>
        <div class="contrast-item">
          <p class="contrast-label mono accent">CIRK asks</p>
          <p class="contrast-question accent">"How should this run?"</p>
        </div>
      </div>
    </div>
  </section>

  <!-- THE SHIFT -->
  <section class="section alt-section" id="why">
    <div class="container">
      <p class="label mono">The shift</p>
      <h2>Story points were built to estimate human effort.</h2>
      <p class="shift-sub">But AI changed the bottleneck. What matters now is not just how long work takes. It is:</p>
      <ul class="shift-list">
        <li>how much context an agent needs</li>
        <li>how many iterations are likely</li>
        <li>how much human review is required</li>
        <li>how risky rollout will be</li>
      </ul>
      <p class="shift-close">CIRK models that reality.</p>
      <p class="shift-close" style="margin-top: 8px; color: var(--text-secondary); font-size: 15px;">Story points governed human delivery for 20 years. CIRK is designed to govern AI delivery.</p>
      <p class="shift-close" style="margin-top: 4px; color: var(--text-secondary); font-size: 14px;">Time is a side effect of the vector — not a dimension of it.</p>
    </div>
  </section>

  <!-- THE MODEL -->
  <section class="section" id="model">
    <div class="container">
      <p class="label mono">The model</p>
      <h2>Four dimensions. One execution policy.</h2>
      <p class="section-sub">Each task is scored from 1 to 3 across four dimensions. The vector defines how it runs.</p>

      <div class="model-formula mono">[ C, I, R, K ] → Execution Decision</div>

      <div class="dimensions-grid">
        <div class="dim-card">
          <div class="dim-letter accent">C</div>
          <h3>Context</h3>
          <p>How much context does the AI need?</p>
          <table>
            <tr><td class="score mono">C1</td><td>Isolated logic, local change, minimal context</td></tr>
            <tr><td class="score mono">C2</td><td>Multiple components, shared patterns, moderate system understanding</td></tr>
            <tr><td class="score mono">C3</td><td>Cross-system behavior, architectural reasoning, or domain-critical context</td></tr>
          </table>
        </div>
        <div class="dim-card">
          <div class="dim-letter" style="color: var(--green)">I</div>
          <h3>Iteration</h3>
          <p>How many iterations until it works?</p>
          <table>
            <tr><td class="score mono">I1</td><td>Deterministic or near one-pass execution</td></tr>
            <tr><td class="score mono">I2</td><td>Some iteration expected, usually 2–3 cycles</td></tr>
            <tr><td class="score mono">I3</td><td>Ambiguous, exploratory, or high-complexity convergence</td></tr>
          </table>
        </div>
        <div class="dim-card">
          <div class="dim-letter" style="color: var(--amber)">R</div>
          <h3>Review</h3>
          <p>How much human review is required?</p>
          <table>
            <tr><td class="score mono">R1</td><td>Quick validation or spot-check</td></tr>
            <tr><td class="score mono">R2</td><td>Rule-level or functional review required</td></tr>
            <tr><td class="score mono">R3</td><td>Deep architectural, security, or mission-critical review</td></tr>
          </table>
        </div>
        <div class="dim-card">
          <div class="dim-letter" style="color: var(--purple)">K</div>
          <h3>Integration Risk</h3>
          <p>How dangerous is deploying this?</p>
          <table>
            <tr><td class="score mono">K1</td><td>Safe, additive, low-risk rollout</td></tr>
            <tr><td class="score mono">K2</td><td>Existing behavior changes, moderate release caution</td></tr>
            <tr><td class="score mono">K3</td><td>Coordinated rollout, migration, or cross-team dependency</td></tr>
          </table>
        </div>
      </div>

      <!-- Score table -->
      <div class="score-table-wrap">
        <p class="label mono">Composite score = C + I + R + K</p>
        <div class="score-table">
          <div class="score-row">
            <span class="mono score-range">4–5</span>
            <span class="badge trivial">Trivial</span>
            <span class="score-note">Autonomous execution — no human intervention needed</span>
          </div>
          <div class="score-row">
            <span class="mono score-range">6–7</span>
            <span class="badge simple">Simple</span>
            <span class="score-note">Guided execution — human review before merge</span>
          </div>
          <div class="score-row">
            <span class="mono score-range">8–9</span>
            <span class="badge medium">Medium</span>
            <span class="score-note">Draft-first or guided — no auto-merge</span>
          </div>
          <div class="score-row">
            <span class="mono score-range">10–11</span>
            <span class="badge complex">Complex</span>
            <span class="score-note">Supervised — approval per step, deploy plan required</span>
          </div>
          <div class="score-row">
            <span class="mono score-range">12</span>
            <span class="badge split">Split Required</span>
            <span class="score-note">Execution blocked — task must be decomposed first</span>
          </div>
        </div>
        <p class="score-note-footer">The vector matters more than the score. Two tasks with the same total may require completely different execution policies.</p>
      </div>

      <!-- Scoring quote -->
      <blockquote class="scoring-quote">
        <p>"Score the execution reality, not the political preference."</p>
        <cite>CIRK Scoring Guidance</cite>
      </blockquote>

      <!-- Inline example -->
      <div class="inline-example">
        <p class="label mono">Example — Login API</p>
        <div class="inline-ex-vector mono">C2 I2 R3 K1 <span class="inline-ex-score">→ Score 8</span></div>
        <div class="inline-ex-rules">
          <span class="ex-b-item ok">✓ Agent can prepare implementation</span>
          <span class="ex-b-item warn">⚠ Human review is mandatory</span>
          <span class="ex-b-item warn">⚠ Auto-merge is disabled</span>
        </div>
      </div>

      <!-- What CIRK is not -->
      <div class="not-cirk">
        <p class="label mono">What CIRK is not</p>
        <div class="not-cirk-grid">
          <div class="not-item">
            <span class="not-x">✕</span>
            <span>A time estimate</span>
          </div>
          <div class="not-item">
            <span class="not-x">✕</span>
            <span>A velocity metric</span>
          </div>
          <div class="not-item">
            <span class="not-x">✕</span>
            <span>A sprint planning abstraction</span>
          </div>
          <div class="not-item">
            <span class="not-x">✕</span>
            <span>A product or platform</span>
          </div>
        </div>
        <p class="not-cirk-close">CIRK is an execution standard for AI-native development.</p>
      </div>

    </div>
  </section>

  <!-- HOW TO USE -->
  <section class="section alt-section">
    <div class="container narrow">
      <p class="label mono">CIRK in 60 seconds</p>
      <h2>Four steps.</h2>
      <div class="steps">
        <div class="step">
          <div class="step-num mono">01</div>
          <div>
            <h3>Pick a task</h3>
            <p>Any unit of work: a feature, a fix, a refactor, a migration.</p>
          </div>
        </div>
        <div class="step">
          <div class="step-num mono">02</div>
          <div>
            <h3>Score it</h3>
            <p>Assign C, I, R, K values from 1 to 3. Ask: how much context? how many cycles? how much review? how risky is rollout?</p>
          </div>
        </div>
        <div class="step">
          <div class="step-num mono">03</div>
          <div>
            <h3>Apply execution policy</h3>
            <p>The vector maps to an execution mode — autonomous, guided, draft-first, supervised, or blocked. Agents and humans follow the same rules.</p>
          </div>
        </div>
        <div class="step">
          <div class="step-num mono">04</div>
          <div>
            <h3>Use it consistently</h3>
            <p>Teams gain a shared language: <span class="mono step-quote">"This is high R."</span> <span class="mono step-quote">"Low K, we can ship."</span> <span class="mono step-quote">"I3 — let the agent draft first."</span></p>
          </div>
        </div>
      </div>

      <div class="thumb-rules">
        <p class="label mono">Rule of thumb</p>
        <div class="thumb-grid">
          <div class="thumb-item"><span class="mono thumb-signal" style="color:var(--amber)">High R</span><span>→ more human review</span></div>
          <div class="thumb-item"><span class="mono thumb-signal" style="color:var(--purple)">High K</span><span>→ tighter deploy controls</span></div>
          <div class="thumb-item"><span class="mono thumb-signal" style="color:var(--green)">High I</span><span>→ more draft-first behavior</span></div>
          <div class="thumb-item"><span class="mono thumb-signal" style="color:var(--accent)">High C</span><span>→ more context preparation</span></div>
        </div>
      </div>
    </div>
  </section>

  <!-- EXECUTION MAPPING -->
  <section class="section" id="execution">
    <div class="container">
      <p class="label mono">Execution mapping</p>
      <h2>CIRK as policy-as-code.</h2>
      <p class="section-sub">The vector defines what happens — not just how big the task is.</p>

      <div class="exec-grid">
        <div class="exec-card">
          <div class="exec-mode mono">Autonomous</div>
          <div class="exec-condition mono">Score 4–5 · R1</div>
          <p>Agent executes without intervention. Auto-approval allowed. No checkpoints.</p>
        </div>
        <div class="exec-card">
          <div class="exec-mode mono">Guided</div>
          <div class="exec-condition mono">Score 6–9</div>
          <p>Agent executes. Human review required before merge.</p>
        </div>
        <div class="exec-card">
          <div class="exec-mode mono">Draft-First</div>
          <div class="exec-condition mono">I3 or R3</div>
          <p>Agent produces a draft. Human validates before any commit.</p>
        </div>
        <div class="exec-card">
          <div class="exec-mode mono">Supervised</div>
          <div class="exec-condition mono">Score ≥ 10 · K3</div>
          <p>Step-by-step execution. Approval per step. Deploy runbook required.</p>
        </div>
        <div class="exec-card blocked">
          <div class="exec-mode mono">Blocked</div>
          <div class="exec-condition mono">Score = 12</div>
          <p>Task must be decomposed. Execution not allowed.</p>
        </div>
      </div>

      <div class="policy-rules">
        <p class="label mono">Policy rules</p>
        <div class="rule-row"><span class="mono rule-signal">I3</span><span>Draft-first required</span></div>
        <div class="rule-row"><span class="mono rule-signal">R3</span><span>Auto-approval disabled</span></div>
        <div class="rule-row"><span class="mono rule-signal">K3</span><span>Deploy runbook required</span></div>
        <div class="rule-row"><span class="mono rule-signal">C3</span><span>Context preparation should be explicit</span></div>
      </div>
    </div>
  </section>

  <!-- QUICK START -->
  <section class="section" id="quick-start">
    <div class="container narrow center">
      <p class="label mono">Quick start</p>
      <h2>How to start using CIRK.</h2>
      <div class="qs-steps">
        <div class="qs-step">
          <span class="qs-num mono">1</span>
          <div>
            <p class="qs-title">Pick a task</p>
            <p class="qs-desc">Any task your team or agent is about to execute.</p>
          </div>
        </div>
        <div class="qs-step">
          <span class="qs-num mono">2</span>
          <div>
            <p class="qs-title">Score it</p>
            <p class="qs-desc"><span class="mono">C(1-3) I(1-3) R(1-3) K(1-3)</span></p>
          </div>
        </div>
        <div class="qs-step">
          <span class="qs-num mono">3</span>
          <div>
            <p class="qs-title">Apply the policy</p>
            <div class="qs-policies">
              <span class="qs-policy"><span class="mono qs-range" style="color:var(--green)">≤ 5</span> automate</span>
              <span class="qs-policy"><span class="mono qs-range" style="color:var(--amber)">6–8</span> require review</span>
              <span class="qs-policy"><span class="mono qs-range" style="color:var(--error, #ff6b6b)">9+</span> control execution</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- EXAMPLES -->
  <section class="section alt-section" id="examples">
    <div class="container">
      <p class="label mono">Examples</p>
      <h2>CIRK in practice.</h2>
      <p class="section-sub">Each vector maps to concrete execution behavior, with reasoning for each dimension.</p>

      <!-- Column headers -->
      <div class="ex-headers">
        <p class="ex-domain-label mono">Backend</p>
        <p class="ex-domain-label mono">Frontend</p>
        <p class="ex-domain-label mono">Agent</p>
      </div>

      <!-- Flat 3×3 grid — rows auto-align by height -->
      <div class="examples-grid">

        <!-- ROW 1 — Trivial -->
        <div class="example-item">
          <div class="ex-header">
            <span class="ex-vector mono">C1 I1 R1 K1</span>
            <span class="ex-score mono">→ 4</span>
            <span class="badge trivial">Trivial</span>
          </div>
          <p class="ex-task">Fix null check in cart total</p>
          <div class="ex-reasoning">
            <span class="ex-r-item"><span class="mono ex-r-dim accent">C1</span> single function, local fix</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--green)">I1</span> deterministic fix</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--amber)">R1</span> low review burden</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--purple)">K1</span> safe, additive fix</span>
          </div>
          <div class="ex-behavior">
            <span class="ex-b-item ok">✓ Autonomous execution</span>
            <span class="ex-b-item ok">✓ Auto-approval allowed</span>
          </div>
        </div>

        <div class="example-item">
          <div class="ex-header">
            <span class="ex-vector mono">C1 I1 R1 K1</span>
            <span class="ex-score mono">→ 4</span>
            <span class="badge trivial">Trivial</span>
          </div>
          <p class="ex-task">Update button color token</p>
          <div class="ex-reasoning">
            <span class="ex-r-item"><span class="mono ex-r-dim accent">C1</span> local visual change</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--green)">I1</span> deterministic implementation</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--amber)">R1</span> minimal review burden</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--purple)">K1</span> low rollout risk</span>
          </div>
          <div class="ex-behavior">
            <span class="ex-b-item ok">✓ Autonomous execution</span>
            <span class="ex-b-item ok">✓ No checkpoints needed</span>
          </div>
        </div>

        <div class="example-item">
          <div class="ex-header">
            <span class="ex-vector mono">C1 I1 R1 K1</span>
            <span class="ex-score mono">→ 4</span>
            <span class="badge trivial">Trivial</span>
          </div>
          <p class="ex-task">Rename internal variable and update references</p>
          <div class="ex-reasoning">
            <span class="ex-r-item"><span class="mono ex-r-dim accent">C1</span> local, contained change</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--green)">I1</span> deterministic, no ambiguity</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--amber)">R1</span> lightweight validation enough</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--purple)">K1</span> no release risk</span>
          </div>
          <div class="ex-behavior">
            <span class="ex-b-item ok">✓ Agent executes directly</span>
            <span class="ex-b-item ok">✓ Lightweight validation only</span>
          </div>
        </div>

        <!-- ROW 2 — Medium -->
        <div class="example-item">
          <div class="ex-header">
            <span class="ex-vector mono">C2 I2 R3 K1</span>
            <span class="ex-score mono">→ 8</span>
            <span class="badge medium">Medium</span>
          </div>
          <p class="ex-task">Login API</p>
          <div class="ex-reasoning">
            <span class="ex-r-item"><span class="mono ex-r-dim accent">C2</span> moderate context across auth components</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--green)">I2</span> validation and edge cases require iteration</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--amber)">R3</span> security-sensitive — strong review required</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--purple)">K1</span> rollout can remain contained</span>
          </div>
          <div class="ex-behavior">
            <span class="ex-b-item ok">✓ Agent can prepare implementation</span>
            <span class="ex-b-item warn">⚠ Human review mandatory</span>
            <span class="ex-b-item warn">⚠ Auto-merge disabled</span>
          </div>
        </div>

        <div class="example-item">
          <div class="ex-header">
            <span class="ex-vector mono">C2 I3 R2 K2</span>
            <span class="ex-score mono">→ 9</span>
            <span class="badge medium">Medium</span>
          </div>
          <p class="ex-task">Dashboard layout revision</p>
          <div class="ex-reasoning">
            <span class="ex-r-item"><span class="mono ex-r-dim accent">C2</span> touches multiple components</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--green)">I3</span> UX refinement requires iteration</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--amber)">R2</span> functional review, not architectural</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--purple)">K2</span> changes existing behavior</span>
          </div>
          <div class="ex-behavior">
            <span class="ex-b-item warn">⚠ Draft-first required (I3)</span>
            <span class="ex-b-item warn">⚠ Human review before merge</span>
          </div>
        </div>

        <div class="example-item">
          <div class="ex-header">
            <span class="ex-vector mono">C2 I3 R2 K2</span>
            <span class="ex-score mono">→ 9</span>
            <span class="badge medium">Medium</span>
          </div>
          <p class="ex-task">Refactor repository layer for shared patterns</p>
          <div class="ex-reasoning">
            <span class="ex-r-item"><span class="mono ex-r-dim accent">C2</span> shared patterns across modules</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--green)">I3</span> exploratory, convergence uncertain</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--amber)">R2</span> review before full implementation</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--purple)">K2</span> changes existing behavior</span>
          </div>
          <div class="ex-behavior">
            <span class="ex-b-item warn">⚠ Agent proposes plan first (I3)</span>
            <span class="ex-b-item warn">⚠ Review before implementation</span>
          </div>
        </div>

        <!-- ROW 3 — Complex -->
        <div class="example-item">
          <div class="ex-header">
            <span class="ex-vector mono">C2 I2 R3 K3</span>
            <span class="ex-score mono">→ 10</span>
            <span class="badge complex">Complex</span>
          </div>
          <p class="ex-task">Billing webhook change</p>
          <div class="ex-reasoning">
            <span class="ex-r-item"><span class="mono ex-r-dim accent">C2</span> touches business-critical logic</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--green)">I2</span> correctness may require several cycles</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--amber)">R3</span> review is mandatory</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--purple)">K3</span> bad rollout can create financial issues</span>
          </div>
          <div class="ex-behavior">
            <span class="ex-b-item block">✕ Supervised execution</span>
            <span class="ex-b-item block">✕ Approval per step</span>
            <span class="ex-b-item block">✕ Deploy runbook required</span>
          </div>
        </div>

        <div class="example-item">
          <div class="ex-header">
            <span class="ex-vector mono">C3 I3 R3 K2</span>
            <span class="ex-score mono">→ 11</span>
            <span class="badge complex">Complex</span>
          </div>
          <p class="ex-task">Auth UI redesign (login + recovery flows)</p>
          <div class="ex-reasoning">
            <span class="ex-r-item"><span class="mono ex-r-dim accent">C3</span> cross-flow understanding required</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--green)">I3</span> multiple design and functional iterations</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--amber)">R3</span> UX + correctness review required</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--purple)">K2</span> changes a critical entry point</span>
          </div>
          <div class="ex-behavior">
            <span class="ex-b-item block">✕ Supervised execution</span>
            <span class="ex-b-item block">✕ Approval per step</span>
            <span class="ex-b-item block">✕ Architect sign-off required</span>
          </div>
        </div>

        <div class="example-item">
          <div class="ex-header">
            <span class="ex-vector mono">C3 I2 R3 K3</span>
            <span class="ex-score mono">→ 11</span>
            <span class="badge complex">Complex</span>
          </div>
          <p class="ex-task">Migrate payment gateway provider</p>
          <div class="ex-reasoning">
            <span class="ex-r-item"><span class="mono ex-r-dim accent">C3</span> cross-system payment flow understanding</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--green)">I2</span> multiple verification cycles expected</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--amber)">R3</span> mandatory human review</span>
            <span class="ex-r-item"><span class="mono ex-r-dim" style="color:var(--purple)">K3</span> rollout must be staged and monitored</span>
          </div>
          <div class="ex-behavior">
            <span class="ex-b-item block">✕ Supervised execution</span>
            <span class="ex-b-item block">✕ Staged rollout required</span>
            <span class="ex-b-item block">✕ Deploy runbook mandatory</span>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- OPEN STANDARD -->
  <section class="section">
    <div class="container narrow center">
      <p class="label mono">Open standard</p>
      <h2>CIRK is not a product.</h2>
      <p class="section-sub">
        It is an open standard for execution governance in AI-native software development.
      </p>
      <p class="open-body">
        It can be adopted in issue trackers, coding agents, pull request workflows,
        internal platforms, or governance layers such as
        <a href="https://orbit618.com" target="_blank" rel="noopener">Orbit618</a>.
      </p>
      <div class="open-grid">
        <div class="open-card">Issue trackers</div>
        <div class="open-card">AI coding agents</div>
        <div class="open-card">PR workflows</div>
        <div class="open-card">Internal platforms</div>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="section alt-section" id="faq">
    <div class="container narrow">
      <p class="label mono">FAQ</p>
      <h2>Common questions.</h2>

      <div class="faq-list">

        <details class="faq-item">
          <summary>
            <span>Is CIRK a replacement for story points?</span>
            <span class="faq-icon mono">+</span>
          </summary>
          <div class="faq-body">
            <p>Yes, in AI-assisted development contexts where execution governance matters more than effort estimation.</p>
            <p>Story points estimate human effort. CIRK estimates execution conditions for AI-native workflows.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>
            <span>Does CIRK estimate time?</span>
            <span class="faq-icon mono">+</span>
          </summary>
          <div class="faq-body">
            <p>Not directly. CIRK is about execution complexity, review intensity, and rollout risk.</p>
            <p>Teams may later derive time insights from calibration data, but duration is not the primary output.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>
            <span>Why doesn't CIRK include a time dimension?</span>
            <span class="faq-icon mono">+</span>
          </summary>
          <div class="faq-body">
            <p>Time obscures the real constraint. CIRK makes it explicit.</p>
            <p>Duration is a byproduct of complexity, iteration depth, review burden, and rollout sensitivity — not an independent variable. Adding time to the model would conflate cause and effect.</p>
            <p>Teams that need execution windows can derive them from the vector: high <span class="mono faq-code">R</span> means longer review cycles, high <span class="mono faq-code">K</span> means wider rollout windows, high <span class="mono faq-code">I</span> means more iteration rounds. The vector already encodes the information — time just reads it.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>
            <span>Can CIRK be used without coding agents?</span>
            <span class="faq-icon mono">+</span>
          </summary>
          <div class="faq-body">
            <p>Yes. Even when humans perform the implementation, CIRK still helps classify review burden, context depth, and deployment sensitivity.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>
            <span>Is CIRK tied to Orbit618?</span>
            <span class="faq-icon mono">+</span>
          </summary>
          <div class="faq-body">
            <p>No. Orbit618 is one possible implementation environment for CIRK, but CIRK is designed as a standalone open standard.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>
            <span>Why not keep story points and add AI rules separately?</span>
            <span class="faq-icon mono">+</span>
          </summary>
          <div class="faq-body">
            <p>Because effort is no longer the most important variable for AI-assisted execution.</p>
            <p>CIRK makes the real constraints explicit instead of treating them as side rules layered on top of an effort model.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>
            <span>What is the most important part of CIRK?</span>
            <span class="faq-icon mono">+</span>
          </summary>
          <div class="faq-body">
            <p>The vector, not just the score.</p>
            <p><span class="mono faq-code">C3 I1 R1 K3</span> and <span class="mono faq-code">C1 I3 R3 K1</span> may have the same sum but require very different execution policies. One is deploy-sensitive. The other is review-sensitive.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>
            <span>Is CIRK a framework or a standard?</span>
            <span class="faq-icon mono">+</span>
          </summary>
          <div class="faq-body">
            <p>CIRK is a standard — not a framework, not a tool. It defines a shared language for execution governance. Any team, tool, or platform can implement it. MIT licensed.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>
            <span>How do I start using CIRK?</span>
            <span class="faq-icon mono">+</span>
          </summary>
          <div class="faq-body">
            <p>Pick a task. Score it across C, I, R, K (1–3 each). Sum the vector. Apply the policy: <span class="mono faq-code">≤5</span> automate, <span class="mono faq-code">6–8</span> require review, <span class="mono faq-code">9+</span> control execution. That's it.</p>
          </div>
        </details>

        <details class="faq-item">
          <summary>
            <span>Does CIRK work with Scrum?</span>
            <span class="faq-icon mono">+</span>
          </summary>
          <div class="faq-body">
            <p>Yes. CIRK replaces the estimation layer (story points), not the process. Sprints, standups, and backlogs stay the same. The difference is that each task carries an execution policy instead of an effort guess.</p>
          </div>
        </details>

      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="section cta-section">
    <div class="container narrow center">
      <h2>Start using CIRK today.</h2>
      <p class="section-sub">MIT licensed. No dependencies. Works with anything.</p>
      <div class="hero-cta">
        <a href="https://github.com/core-618/cirk" class="btn-primary" target="_blank" rel="noopener">Read the spec →</a>
        <a href="#examples" class="btn-ghost">View examples</a>
        <a href="https://github.com/core-618/cirk" class="btn-ghost" target="_blank" rel="noopener">Open GitHub</a>
      </div>
    </div>
  </section>

  <!-- FOOTER TAGLINE -->
  <div class="footer-tagline">
    <div class="container">
      <span class="mono">AI writes the code. CIRK governs the execution.</span>
    </div>
  </div>

  <!-- FOOTER -->
  <footer>
    <div class="container footer-inner">
      <span class="mono footer-logo">CIRK</span>
      <span class="footer-meta">MIT License · <a href="https://github.com/core-618/cirk" target="_blank" rel="noopener">GitHub</a> · <a href="mailto:hello@core618.com">hello@core618.com</a></span>
      <span class="footer-meta">A standard by <a href="https://core618.com" target="_blank" rel="noopener">Core618</a></span>
    </div>
  </footer>
</Layout>

<style>
  /* ── Nav ── */
  nav {
    position: sticky; top: 0; z-index: 100;
    background: rgba(17,17,17,0.92);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
  }
  .nav-inner {
    max-width: 1100px; margin: 0 auto; padding: 0 24px;
    height: 56px; display: flex; align-items: center; justify-content: space-between;
  }
  .nav-logo { font-size: 14px; font-weight: 500; color: var(--text); letter-spacing: 0.08em; }
  .nav-links { display: flex; gap: 28px; }
  .nav-links a { font-size: 13px; color: var(--text-muted); transition: color 0.15s; }
  .nav-links a:hover { color: var(--text); text-decoration: none; }

  /* ── Layout ── */
  .container { max-width: 1100px; margin: 0 auto; padding: 0 24px; }
  .container.narrow { max-width: 760px; }
  .container.center { text-align: center; }
  .section { padding: 72px 0; }
  .alt-section { background: var(--bg-container); }

  /* ── Hero ── */
  .hero { padding: 96px 0 64px; }
  .hero-status {
    display: inline-flex; align-items: center; gap: 8px;
    font-size: 11px; letter-spacing: 0.08em; text-transform: uppercase;
    color: var(--green); background: var(--green-dim);
    border: 1px solid rgba(16,185,129,0.25);
    border-radius: 20px; padding: 5px 14px; margin-bottom: 32px;
  }
  .hero-status-dot {
    width: 6px; height: 6px; border-radius: 50%;
    background: var(--green); flex-shrink: 0;
    box-shadow: 0 0 6px var(--green);
  }
  .hero h1 {
    font-size: clamp(2rem, 5vw, 3.5rem); font-weight: 500;
    line-height: 1.15; letter-spacing: -0.02em; margin-bottom: 20px; color: var(--text);
  }
  .hero-clarify {
    font-size: clamp(1rem, 2vw, 1.25rem); color: var(--text-muted);
    font-weight: 400; margin-bottom: 40px; line-height: 1.5;
  }
  /* Hero model summary */
  .hero-model {
    display: flex; flex-direction: column; gap: 10px;
    margin-bottom: 32px; max-width: 560px;
  }
  .hero-dim { display: flex; align-items: baseline; gap: 12px; }
  .hero-dim-letter { font-size: 15px; font-weight: 700; min-width: 16px; }
  .hero-dim-name { font-size: 14px; font-weight: 500; color: var(--text); min-width: 72px; }
  .hero-dim-desc { font-size: 13px; color: var(--text-muted); }
  /* Hero example */
  .hero-example {
    display: inline-flex; flex-direction: column; gap: 8px;
    background: var(--bg-card); border: 1px solid var(--border);
    border-radius: var(--radius); padding: 20px 24px;
    margin-bottom: 32px; font-size: 13px;
  }
  .hero-ex-task { color: var(--text-secondary); margin-bottom: 4px; }
  .hero-ex-vector { display: flex; align-items: center; gap: 10px; font-size: 15px; font-weight: 500; flex-wrap: wrap; }
  .hero-ex-sep { color: var(--border); }
  .hero-ex-result { color: var(--text-muted); margin-top: 4px; }
  /* Tagline */
  .hero-tagline {
    font-size: 15px; color: var(--text-secondary); font-style: italic;
    margin-bottom: 28px; font-weight: 400;
  }
  .hero-cta { display: flex; gap: 12px; flex-wrap: wrap; }
  .btn-primary {
    display: inline-flex; align-items: center; padding: 10px 20px;
    background: var(--accent); color: #fff; border-radius: var(--radius-sm);
    font-size: 14px; font-weight: 500; transition: opacity 0.15s;
  }
  .btn-primary:hover { opacity: 0.85; text-decoration: none; }
  .btn-ghost {
    display: inline-flex; align-items: center; padding: 10px 20px;
    background: transparent; color: var(--text-muted);
    border: 1px solid var(--border); border-radius: var(--radius-sm);
    font-size: 14px; font-weight: 500; transition: border-color 0.15s, color 0.15s;
  }
  .btn-ghost:hover { border-color: var(--border-hover); color: var(--text); text-decoration: none; }

  /* ── Typography ── */
  .label {
    font-size: 11px; letter-spacing: 0.1em; text-transform: uppercase;
    color: var(--text-secondary); margin-bottom: 16px; display: block;
  }
  h2 {
    font-size: clamp(1.5rem, 3vw, 2.25rem); font-weight: 500;
    letter-spacing: -0.02em; line-height: 1.2; margin-bottom: 8px;
  }
  h3 { font-size: 1rem; font-weight: 500; margin-bottom: 8px; }
  .accent { color: var(--accent); }
  .section-sub {
    font-size: 1rem; color: var(--text-muted); margin-top: 12px;
    margin-bottom: 48px; line-height: 1.7;
  }
  .mono { font-family: var(--font-mono); }

  /* ── Shift ── */
  #why .container { text-align: right; }
  .shift-sub { font-size: 1rem; color: var(--text-muted); margin: 16px 0 20px; line-height: 1.7; }
  .shift-list { list-style: none; display: flex; flex-direction: column; gap: 10px; margin-bottom: 28px; }
  .shift-list li { font-size: 15px; color: var(--text); }
  .shift-close { font-size: 1.125rem; font-weight: 500; color: var(--accent); }

  /* ── Contrast (Quick Explanation) ── */
  .contrast-section { padding: 48px 0; }
  .contrast-block { display: flex; align-items: center; justify-content: center; gap: 48px; }
  .contrast-item { flex: 1; max-width: 320px; }
  .contrast-label { font-size: 13px; letter-spacing: 0.08em; text-transform: uppercase; color: var(--text-secondary); margin-bottom: 8px; }
  .contrast-question { font-size: 1.5rem; font-weight: 500; color: var(--text-muted); line-height: 1.3; }
  .contrast-question.accent { color: var(--accent); }
  .contrast-divider { width: 1px; height: 64px; background: var(--border); flex-shrink: 0; }

  /* ── Model ── */
  .model-formula { font-size: clamp(1.25rem, 3vw, 2rem); color: var(--text-secondary); margin: 40px 0 48px; letter-spacing: 0.06em; }
  .dimensions-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; margin-bottom: 40px; }
  .dim-card { background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius); padding: 24px 20px; }
  .dim-letter { font-family: var(--font-mono); font-size: 2rem; font-weight: 500; margin-bottom: 8px; }
  .dim-card > p { font-size: 13px; color: var(--text-muted); margin-bottom: 16px; line-height: 1.5; }
  .dim-card table { width: 100%; border-collapse: collapse; }
  .dim-card td { font-size: 12px; padding: 5px 0; vertical-align: top; line-height: 1.4; }
  .dim-card td:first-child { color: var(--text-secondary); width: 28px; padding-right: 8px; }
  .dim-card td:last-child { color: var(--text-muted); }

  /* ── Score table ── */
  .score-table-wrap { background: var(--bg-container); border: 1px solid var(--border); border-radius: var(--radius); padding: 32px; margin-bottom: 32px; }
  .score-table { display: flex; flex-direction: column; }
  .score-row { display: flex; align-items: center; gap: 16px; padding: 12px 0; border-bottom: 1px solid var(--border); }
  .score-row:last-child { border-bottom: none; }
  .score-range { min-width: 44px; color: var(--text-secondary); font-size: 13px; }
  .score-note { font-size: 13px; color: var(--text-muted); }
  .score-note-footer { margin-top: 20px; font-size: 13px; color: var(--text-secondary); font-style: italic; line-height: 1.5; }

  /* ── Scoring quote ── */
  .scoring-quote {
    border-left: 2px solid var(--accent);
    padding: 16px 24px; margin: 0 0 32px;
    background: var(--accent-dim);
    border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
  }
  .scoring-quote p { font-size: 1rem; color: var(--text); font-style: italic; margin-bottom: 6px; }
  .scoring-quote cite { font-size: 12px; color: var(--text-secondary); font-style: normal; font-family: var(--font-mono); letter-spacing: 0.04em; }

  /* ── Inline example ── */
  .inline-example { padding: 24px 28px; background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius); margin-bottom: 32px; }
  .inline-ex-vector { font-size: 1.125rem; font-weight: 500; color: var(--text); margin-bottom: 16px; }
  .inline-ex-score { color: var(--text-secondary); font-weight: 400; }
  .inline-ex-rules { display: flex; flex-wrap: wrap; gap: 8px; }

  /* ── What CIRK is not ── */
  .not-cirk { padding: 28px; background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius); }
  .not-cirk-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 12px; margin: 20px 0; }
  .not-item { display: flex; align-items: center; gap: 10px; font-size: 14px; color: var(--text-muted); }
  .not-x { color: var(--text-secondary); font-family: var(--font-mono); font-size: 13px; flex-shrink: 0; }
  .not-cirk-close { font-size: 14px; color: var(--text); font-weight: 500; margin-top: 4px; }

  /* ── Badges ── */
  .badge {
    font-family: var(--font-mono); font-size: 11px; padding: 2px 10px;
    border-radius: 20px; white-space: nowrap; flex-shrink: 0;
  }
  .badge.trivial { background: var(--green-dim);  color: var(--green);   border: 1px solid rgba(16,185,129,0.2); }
  .badge.simple  { background: var(--accent-dim); color: var(--accent);  border: 1px solid var(--accent-border); }
  .badge.medium  { background: var(--amber-dim);  color: var(--amber);   border: 1px solid rgba(245,158,11,0.2); }
  .badge.complex { background: rgba(239,68,68,0.1); color: #ef4444;      border: 1px solid rgba(239,68,68,0.2); }
  .badge.split   { background: rgba(139,92,246,0.1); color: var(--purple); border: 1px solid rgba(139,92,246,0.2); }

  /* ── Steps ── */
  .steps { display: flex; flex-direction: column; border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; margin-bottom: 40px; }
  .step { display: flex; align-items: flex-start; gap: 24px; padding: 24px 28px; background: var(--bg-card); border-bottom: 1px solid var(--border); }
  .step:last-child { border-bottom: none; }
  .step-num { font-size: 1.5rem; font-weight: 500; color: var(--accent); min-width: 36px; line-height: 1.3; }
  .step h3 { margin-bottom: 6px; }
  .step p { font-size: 14px; color: var(--text-muted); line-height: 1.6; }
  .step-quote { font-size: 12px; background: var(--bg-container); color: var(--text-secondary); padding: 2px 6px; border-radius: 4px; display: inline-block; margin: 2px 2px 0 0; }

  /* ── Rule of thumb ── */
  .thumb-rules { border: 1px solid var(--border); border-radius: var(--radius); padding: 24px 28px; background: var(--bg-card); }
  .thumb-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-top: 4px; }
  .thumb-item { display: flex; align-items: center; gap: 12px; font-size: 14px; color: var(--text-muted); }
  .thumb-signal { font-size: 13px; font-weight: 500; min-width: 52px; }

  /* ── Execution ── */
  .exec-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 16px; margin-bottom: 40px; }
  .exec-card { background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius); padding: 24px 20px; }
  .exec-card.blocked { border-color: rgba(139,92,246,0.3); }
  .exec-mode { font-size: 13px; font-weight: 500; color: var(--text); margin-bottom: 6px; letter-spacing: 0.04em; }
  .exec-condition { font-size: 12px; color: var(--accent); margin-bottom: 12px; }
  .exec-card p { font-size: 13px; color: var(--text-muted); line-height: 1.6; }
  .policy-rules { display: flex; flex-direction: column; border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; margin: 0 auto; max-width: 540px; }
  .rule-row { display: flex; align-items: center; gap: 16px; padding: 14px 20px; background: var(--bg-card); border-bottom: 1px solid var(--border); }
  .rule-row:last-child { border-bottom: none; }
  .rule-signal { color: var(--amber); font-size: 13px; min-width: 28px; }
  .rule-row span:last-child { font-size: 13px; color: var(--text-muted); }

  /* ── Quick Start ── */
  .qs-steps { display: flex; flex-direction: column; gap: 24px; margin-top: 40px; max-width: 480px; margin-left: auto; margin-right: auto; text-align: left; }
  .qs-step { display: flex; gap: 20px; align-items: flex-start; }
  .qs-num { font-size: 20px; font-weight: 600; color: var(--accent); min-width: 28px; line-height: 1.4; }
  .qs-title { font-size: 16px; font-weight: 500; color: var(--text); margin-bottom: 4px; }
  .qs-desc { font-size: 14px; color: var(--text-muted); }
  .qs-policies { display: flex; flex-direction: column; gap: 6px; margin-top: 6px; }
  .qs-policy { font-size: 14px; color: var(--text-muted); }
  .qs-range { font-weight: 500; margin-right: 8px; }

  /* ── Examples ── */
  .ex-headers { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin-bottom: 4px; }
  .ex-domain-label { font-size: 11px; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-secondary); margin-bottom: 4px; display: block; }
  .examples-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; align-items: stretch; }
  .example-item { padding: 20px 22px; background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius); display: flex; flex-direction: column; }
  .ex-header { display: flex; align-items: center; gap: 10px; margin-bottom: 4px; flex-wrap: wrap; }
  .ex-vector { font-size: 13px; color: var(--text); font-weight: 500; }
  .ex-score  { font-size: 13px; color: var(--text-secondary); }
  .ex-task   { font-size: 13px; color: var(--text-secondary); margin-bottom: 12px; }
  .ex-reasoning { display: flex; flex-direction: column; gap: 4px; margin-bottom: 12px; padding: 12px; background: var(--bg-container); border-radius: var(--radius-sm); }
  .ex-r-item { font-size: 12px; color: var(--text-muted); display: flex; align-items: baseline; gap: 8px; }
  .ex-r-dim  { font-size: 12px; font-weight: 500; min-width: 24px; }
  .ex-behavior { display: flex; flex-wrap: wrap; gap: 6px; }
  .ex-b-item { font-family: var(--font-mono); font-size: 11px; padding: 3px 10px; border-radius: var(--radius-sm); white-space: nowrap; }
  .ex-b-item.ok    { background: var(--green-dim);          color: var(--green);  border: 1px solid rgba(16,185,129,0.2); }
  .ex-b-item.warn  { background: var(--amber-dim);          color: var(--amber);  border: 1px solid rgba(245,158,11,0.2); }
  .ex-b-item.block { background: rgba(239,68,68,0.08);      color: #ef4444;       border: 1px solid rgba(239,68,68,0.2); }

  /* ── Open standard ── */
  .open-grid { display: flex; flex-wrap: wrap; gap: 8px; justify-content: center; margin: 28px 0; }
  .open-card { font-size: 13px; color: var(--text-muted); background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 8px 16px; }
  .open-body { font-size: 14px; color: var(--text-muted); line-height: 1.7; margin-bottom: 4px; margin-top: -12px; }

  /* ── FAQ ── */
  .faq-list { display: flex; flex-direction: column; border: 1px solid var(--border); border-radius: var(--radius); overflow: hidden; margin-top: 40px; }
  .faq-item { border-bottom: 1px solid var(--border); }
  .faq-item:last-child { border-bottom: none; }
  .faq-item summary {
    display: flex; align-items: center; justify-content: space-between;
    padding: 20px 24px; cursor: pointer; list-style: none;
    font-size: 15px; font-weight: 500; color: var(--text);
    background: var(--bg-card);
    transition: background 0.15s;
    gap: 16px;
  }
  .faq-item summary::-webkit-details-marker { display: none; }
  .faq-item summary:hover { background: var(--bg-card-hover); }
  .faq-icon {
    font-size: 18px; color: var(--text-secondary); flex-shrink: 0;
    transition: transform 0.2s, color 0.15s;
    line-height: 1;
  }
  .faq-item[open] summary { background: var(--bg-card-hover); }
  .faq-item[open] .faq-icon { transform: rotate(45deg); color: var(--accent); }
  .faq-body {
    padding: 0 24px 20px;
    background: var(--bg-card-hover);
    display: flex; flex-direction: column; gap: 8px;
  }
  .faq-body p { font-size: 14px; color: var(--text-muted); line-height: 1.7; }
  .faq-code { background: var(--bg-highlight); color: var(--text); padding: 2px 7px; border-radius: 4px; font-size: 13px; }

  /* ── CTA ── */
  .cta-section h2 { margin-bottom: 8px; }
  .cta-section .section-sub { margin-bottom: 32px; }
  .cta-section .hero-cta { justify-content: center; }

  /* ── Footer tagline ── */
  .footer-tagline {
    background: var(--bg-container); border-top: 1px solid var(--border);
    padding: 20px 0; text-align: center;
  }
  .footer-tagline span { font-size: 12px; color: var(--text-secondary); letter-spacing: 0.04em; }

  /* ── Footer ── */
  footer { border-top: 1px solid var(--border); padding: 32px 0; }
  .footer-inner { display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 12px; }
  .footer-logo { font-size: 13px; color: var(--text-secondary); letter-spacing: 0.08em; }
  .footer-meta { font-size: 12px; color: var(--text-secondary); }
  .footer-meta a { color: var(--text-secondary); }
  .footer-meta a:hover { color: var(--text); }

  /* ── Responsive ── */
  @media (max-width: 1024px) {
    .dimensions-grid { grid-template-columns: repeat(2, 1fr); }
    .exec-grid { grid-template-columns: repeat(3, 1fr); }
    .examples-grid { grid-template-columns: 1fr; }
    .ex-headers { display: none; }
  }
  @media (max-width: 700px) {
    #why .container { text-align: left; }
    .shift-sub, .shift-list, .shift-close { text-align: left; }
    .dimensions-grid { grid-template-columns: 1fr; }
    .exec-grid { grid-template-columns: 1fr; }
    .not-cirk-grid { grid-template-columns: 1fr; }
    .thumb-grid { grid-template-columns: 1fr; }
    .nav-links { display: none; }
    .contrast-block { flex-direction: column; gap: 24px; }
    .contrast-divider { width: 64px; height: 1px; }
    .footer-inner { flex-direction: column; align-items: flex-start; }
    .hero { padding: 72px 0 64px; }
    .section { padding: 64px 0; }
    .faq-item summary { font-size: 14px; padding: 16px 20px; }
    .faq-body { padding: 0 20px 16px; }
  }
</style>
